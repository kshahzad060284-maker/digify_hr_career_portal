enum LoginToastType {
  emailRequired,
  emailInvalid,
  passwordRequired,
  signInFailed,
}

enum RegisterToastType {
  firstNameRequired,
  lastNameRequired,
  emailRequired,
  emailInvalid,
  phoneRequired,
  passwordRequired,
  confirmPasswordRequired,
  passwordsMismatch,
  createAccountFailed,
}

enum RegisterRelocatePreference { yes, no }
